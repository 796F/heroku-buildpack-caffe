#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

set -e # fail fast

# parse params
BUILD_DIR=$1
CACHE_DIR=$2
BUILDPACK_NAME=caffe

indent() {
  sed -u 's/^/       /'
}

echo "-----> Vendoring binaries" | indent

exit 0